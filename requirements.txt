timm>=1.0.0
Pillow>=10.0.0
pyyaml>=6.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.65.0

# src/face: face recognition, emotion detection, gaze tracking
numpy>=1.21.0
opencv-python>=4.8.0
mediapipe>=0.10.0
face-recognition>=1.3.0
# Optional: pip install git+https://github.com/ageitgey/face_recognition_models
# setuptools<82 needed for pkg_resources (face_recognition_models) on Python 3.14
setuptools<82
torch>=2.0.0

# src/body: body pose detection (YOLO11-pose)
ultralytics>=8.3.0
